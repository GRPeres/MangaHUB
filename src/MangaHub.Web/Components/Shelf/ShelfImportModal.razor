@if (Open)
{
    <div class="mh-modal-backdrop" @onclick="Close">
        <section class="mh-modal mh-small-modal" role="dialog" aria-modal="true" aria-labelledby="shelf-import-title" @onclick:stopPropagation="true">
            <MudStack Spacing="3">
                <MudStack Row="true" AlignItems="AlignItems.Start" Justify="Justify.SpaceBetween" Class="mh-modal-header">
                    <MudStack Spacing="1">
                        <MudText Typo="Typo.overline" Color="Color.Secondary">Shelf import</MudText>
                        <MudText id="shelf-import-title" Typo="Typo.h2">Import shelf CSV</MudText>
                        <MudText Typo="Typo.body2" Color="Color.Secondary">Adds rows to your shelf when the manga already exists in the catalog.</MudText>
                    </MudStack>
                    <MudIconButton Icon="@Icons.Material.Filled.Close" Color="Color.Secondary" OnClick="Close" aria-label="Close shelf import" />
                </MudStack>

                <InputFile OnChange="ImportShelfCsv" accept=".csv,text/csv" />

                @if (!string.IsNullOrWhiteSpace(importMessage))
                {
                    <MudAlert Severity="@importSeverity" Variant="Variant.Outlined">@importMessage</MudAlert>
                }
            </MudStack>
        </section>
    </div>
}
